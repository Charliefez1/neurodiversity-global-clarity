import { ClipboardCheck } from 'lucide-react';
export default function PortalAttendance() {
  return (<div className="max-w-4xl space-y-6"><h1 className="text-2xl font-semibold flex items-center gap-2"><ClipboardCheck className="h-6 w-6 text-primary" />Attendance</h1><p className="text-muted-foreground">Attendance tracking is being rebuilt with live data. Check back soon.</p></div>);
}
