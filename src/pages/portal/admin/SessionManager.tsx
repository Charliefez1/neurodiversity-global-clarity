import { Settings2 } from 'lucide-react';
export default function PortalSessionManager() {
  return (<div className="max-w-4xl space-y-6"><h1 className="text-2xl font-semibold flex items-center gap-2"><Settings2 className="h-6 w-6 text-primary" />Session manager</h1><p className="text-muted-foreground">Session manager is being rebuilt. The full wizard with availability engine integration will be connected once the database tables are migrated.</p></div>);
}
